# 2026-08-16 G1-P0 报表实施：本机 WorkBuddy 沙箱拦截 node 级符号链接创建，
# pnpm 默认符号链接布局（symlinked node_modules）无法在本机工作区落地。
# 切换 hoisted 扁平布局（无符号链接，等价 npm 布局），前后端运行/构建均兼容。
# 注意：CI 与其它机器同样生效；若恢复符号链接能力，可删除本行并重跑 pnpm install。
node-linker=hoisted
